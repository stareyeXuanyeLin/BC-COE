# 已废弃：单图层变换中心重构任务

本文档对应的可调变换中心方案已废弃。相关中心字段、中心拖拽交互、十字光标、坐标逆变换和中心协议同步代码均已删除。

当前实现保留：

- 单图层旋转与缩放，优先围绕纹理 Alpha 有效内容包围盒中心；像素扫描未完成或失败时回退到纹理中点。
- 整体旋转与缩放，固定使用自动计算的整体中心。
- 偏移、衣柜存储和远端静态视觉同步。

当前行为和字段定义请以 `docs/architecture.md`、`src/02-data.js`、`src/07-renderer.js` 以及编辑器实现为准。
